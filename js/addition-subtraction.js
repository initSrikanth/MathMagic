'use strict';const screens=[...document.querySelectorAll('.screen')],tabs=[...document.querySelectorAll('.tab')];function showScreen(id){screens.forEach(s=>s.classList.toggle('active',s.id===id));tabs.forEach(t=>t.classList.toggle('active',t.dataset.screen===id));window.scrollTo({top:0,behavior:'smooth'});if(id==='challenge'&&!quiz.length)startQuiz()}tabs.forEach(t=>t.addEventListener('click',()=>showScreen(t.dataset.screen)));document.querySelectorAll('[data-next]').forEach(b=>b.addEventListener('click',()=>showScreen(b.dataset.next)));document.querySelectorAll('.practice button[data-answer]').forEach(b=>b.addEventListener('click',()=>{const input=b.parentElement.querySelector('input'),fb=b.parentElement.nextElementSibling,ok=input.value.replace(/[$, ]/g,'')===b.dataset.answer;fb.textContent=ok?'Correct!':'Not quite. Answer: '+Number(b.dataset.answer).toLocaleString();fb.className='practice-feedback '+(ok?'correct':'incorrect')}));
const rnd=(a,b)=>Math.floor(Math.random()*(b-a+1))+a,choice=a=>a[rnd(0,a.length-1)],fmt=n=>Number(n).toLocaleString('en-AU');
function numberQ(topic,prompt,display,answer){return{topic,prompt,display,answer:String(answer),type:'number'}}function choiceQ(topic,prompt,display,options,answer){return{topic,prompt,display,options:options.map(String),answer:String(answer),type:'choice'}}
function addQ(min=1000,max=99999){const a=rnd(min,max),b=rnd(min,max);return numberQ('Addition','Calculate exactly.',`${fmt(a)} + ${fmt(b)}`,a+b)}function subQ(min=1000,max=99999){let a=rnd(min,max),b=rnd(min,max);if(b>a)[a,b]=[b,a];return numberQ('Subtraction','Calculate exactly.',`${fmt(a)} − ${fmt(b)}`,a-b)}
function friendly(){const base=rnd(12,80)*100,offset=choice([1,2,5,10,20]),a=base-offset,b=rnd(1200,8500);return numberQ('Mental Strategy','Use a friendly-number strategy.',`${fmt(a)} + ${fmt(b)}`,a+b)}
function closeSub(){const base=rnd(20,90)*100,d1=rnd(1,15),d2=rnd(1,15),a=base+d1,b=base-d2;return numberQ('Mental Strategy','Find the difference efficiently.',`${fmt(a)} − ${fmt(b)}`,a-b)}
function estimateAdd(){const a=rnd(15,89)*1000+rnd(0,999),b=rnd(10,79)*1000+rnd(0,999),ans=Math.round(a/10000)*10000+Math.round(b/10000)*10000;return numberQ('Estimation','Estimate by rounding each number to the nearest ten thousand.',`${fmt(a)} + ${fmt(b)}`,ans)}
function estimateSub(){let a=rnd(50,95)*1000+rnd(0,999),b=rnd(10,45)*1000+rnd(0,999);const ans=Math.round(a/10000)*10000-Math.round(b/10000)*10000;return numberQ('Estimation','Estimate by rounding each number to the nearest ten thousand.',`${fmt(a)} − ${fmt(b)}`,ans)}
function unknownAdd(){const x=rnd(1500,15000),b=rnd(1200,9000),t=x+b;return numberQ('Missing Value','Find the missing number.',`□ + ${fmt(b)} = ${fmt(t)}`,x)}
function unknownSub(){const x=rnd(1200,9000),result=rnd(3000,12000),total=x+result;return numberQ('Missing Value','Find the missing number.',`${fmt(total)} − □ = ${fmt(result)}`,x)}
function inverse(){const a=rnd(2000,15000),b=rnd(1000,9000),t=a+b;return choiceQ('Inverse Operations',`Which equation checks ${fmt(a)} + ${fmt(b)} = ${fmt(t)}?`,'Use the inverse operation.',[`${fmt(t)} − ${fmt(b)} = ${fmt(a)}`,`${fmt(t)} + ${fmt(b)} = ${fmt(a)}`,`${fmt(a)} − ${fmt(b)} = ${fmt(t)}`,`${fmt(t)} − ${fmt(a)} = ${fmt(t)}`],`${fmt(t)} − ${fmt(b)} = ${fmt(a)}`)}
function reasonable(){const a=rnd(35,75)*1000+rnd(0,999),b=rnd(12,30)*1000+rnd(0,999),exact=a+b,near=Math.round(exact/1000)*1000;return choiceQ('Reasonableness','Which answer is the most reasonable estimate?',`${fmt(a)} + ${fmt(b)}`,[near,near+90000,Math.max(0,near-50000),near+400000],near)}
function oneStep(){const total=rnd(18000,45000),have=rnd(7000,total-3000);return numberQ('Problem Solving',`A community project needs $${fmt(total)}. It has raised $${fmt(have)}. How much more is needed?`,'Choose the operation, then calculate.',total-have)}
function twoStep(){const start=rnd(12000,30000),added=rnd(1500,5000),removed=rnd(500,1400);return numberQ('Two-step Problem',`A library had ${fmt(start)} books. It received ${fmt(added)} new books and removed ${fmt(removed)} damaged books. How many books does it have now?`,'Model the two changes.',start+added-removed)}
function errorSpot(){const a=rnd(45,75)*1000,b=rnd(15,30)*1000,correct=a-b;return choiceQ('Reasoning',`A student calculates ${fmt(a)} − ${fmt(b)}. Which result is reasonable?`,'Estimate before choosing.',[correct,correct*10,correct+100000,Math.max(0,correct-30000)],correct)}
function buildQuiz(){return[friendly(),closeSub(),addQ(1000,15000),subQ(1000,15000),estimateAdd(),addQ(),subQ(),estimateSub(),unknownAdd(),inverse(),addQ(),subQ(),unknownSub(),reasonable(),oneStep(),twoStep(),errorSpot(),unknownAdd(),twoStep(),reasonable()]}
let quiz=[],index=0,score=0,answered=false;const qn=document.getElementById('qNumber'),ql=document.getElementById('qLevel'),qs=document.getElementById('qScore'),qp=document.getElementById('quizProgress'),qt=document.getElementById('qTopic'),qprompt=document.getElementById('qPrompt'),qd=document.getElementById('qDisplay'),qa=document.getElementById('qAnswer'),qf=document.getElementById('qFeedback'),check=document.getElementById('checkAnswer'),next=document.getElementById('nextQuestion'),result=document.getElementById('resultCard');function level(){return index<5?'FOUNDATION':index<10?'DEVELOPING':index<15?'PROFICIENT':'CHALLENGE'}
function render(){const q=quiz[index];answered=false;qn.textContent=`${index+1} / 20`;ql.textContent=level();qs.textContent=`${score} / 20`;qp.style.width=`${(index+1)*5}%`;qt.textContent=q.topic;qprompt.textContent=q.prompt;qd.textContent=q.display;qf.textContent='';qf.className='q-feedback';check.disabled=false;next.disabled=true;qa.innerHTML='';if(q.type==='choice'){qa.innerHTML='<div class="choice-wrap">'+q.options.map(o=>`<button type="button" data-choice="${o.replace(/"/g,'&quot;')}">${o}</button>`).join('')+'</div>';qa.querySelectorAll('[data-choice]').forEach(b=>b.addEventListener('click',()=>finishCheck(b.dataset.choice===q.answer,q.answer)))}else{qa.innerHTML='<input id="answerInput" inputmode="numeric" placeholder="Your answer" aria-label="Your answer">'}}
function finishCheck(ok,answer){if(answered)return;answered=true;if(ok){score++;qf.textContent='Correct!';qf.className='q-feedback correct'}else{qf.textContent='Not quite. Correct answer: '+(Number.isFinite(Number(answer))?fmt(Number(answer)):answer);qf.className='q-feedback incorrect'}qs.textContent=`${score} / 20`;check.disabled=true;next.disabled=false;qa.querySelectorAll('input,button').forEach(x=>x.disabled=true)}
check.addEventListener('click',()=>{if(answered)return;const q=quiz[index];if(q.type==='choice'){qf.textContent='Choose one of the answers.';return}const inp=document.getElementById('answerInput');if(!inp||inp.value.trim()===''){qf.textContent='Enter an answer first.';return}const v=inp.value.replace(/[,$ ]/g,'');if(!/^-?\d+$/.test(v)){qf.textContent='Enter a whole number.';return}finishCheck(v===q.answer,q.answer)});next.addEventListener('click',()=>{if(index<19){index++;render()}else finish()});document.getElementById('restartQuiz').addEventListener('click',startQuiz);function startQuiz(){quiz=buildQuiz();index=0;score=0;document.querySelector('.quiz-card').hidden=false;result.hidden=true;render()}function finish(){document.querySelector('.quiz-card').hidden=true;result.hidden=false;const pct=score*5;result.innerHTML=`<p class="page-tag">CHALLENGE COMPLETE</p><h2>${pct}%</h2><strong>${score} out of 20 correct</strong><p>${pct>=85?'Excellent understanding.':pct>=70?'Strong work. Review any concepts you missed.':pct>=50?'Good progress. Revisit the learning pages before another attempt.':'Revisit Pages 1–5, then try the challenge again.'}</p>`}
